/// <reference types="node" />
import { EthAddress, GrumpkinAddress } from '../../address';
import { AssetId } from '../../asset';
import { HashPath } from '../../merkle_tree';
import { AccountAliasId } from '../../account_id';
import { TreeNote, ClaimNoteTxData } from '../../note_algorithms';
import { SchnorrSignature } from '../../crypto/schnorr';
export declare class JoinSplitTx {
    publicInput: bigint;
    publicOutput: bigint;
    assetId: AssetId;
    numInputNotes: number;
    inputNoteIndices: number[];
    merkleRoot: Buffer;
    inputNotePaths: HashPath[];
    inputNotes: TreeNote[];
    outputNotes: TreeNote[];
    claimNote: ClaimNoteTxData;
    accountPrivateKey: Buffer;
    accountAliasId: AccountAliasId;
    accountIndex: number;
    accountPath: HashPath;
    signingPubKey: GrumpkinAddress;
    signature: SchnorrSignature;
    inputOwner: EthAddress;
    outputOwner: EthAddress;
    constructor(publicInput: bigint, publicOutput: bigint, assetId: AssetId, numInputNotes: number, inputNoteIndices: number[], merkleRoot: Buffer, inputNotePaths: HashPath[], inputNotes: TreeNote[], outputNotes: TreeNote[], claimNote: ClaimNoteTxData, accountPrivateKey: Buffer, accountAliasId: AccountAliasId, accountIndex: number, accountPath: HashPath, signingPubKey: GrumpkinAddress, signature: SchnorrSignature, inputOwner: EthAddress, outputOwner: EthAddress);
    toBuffer(): Buffer;
}
//# sourceMappingURL=join_split_tx.d.ts.map