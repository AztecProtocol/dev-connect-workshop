/// <reference types="node" />
import { GrumpkinAddress } from '@aztec/barretenberg/address';
import { AccountProver, AccountTx } from '@aztec/barretenberg/client_proofs';
import { Pedersen } from '@aztec/barretenberg/crypto';
import { WorldState } from '@aztec/barretenberg/world_state';
import { AliasHash } from '@aztec/barretenberg/account_id';
import { Signer } from '../../signer';
export declare class AccountProofCreator {
    private accountProver;
    private worldState;
    private pedersen;
    constructor(accountProver: AccountProver, worldState: WorldState, pedersen: Pedersen);
    createAccountTx(signer: Signer, aliasHash: AliasHash, nonce: number, migrate: boolean, accountPublicKey: GrumpkinAddress, newAccountPublicKey?: GrumpkinAddress, newSigningPubKey1?: GrumpkinAddress, newSigningPubKey2?: GrumpkinAddress, accountIndex?: number): Promise<AccountTx>;
    createProof(signer: Signer, aliasHash: AliasHash, nonce: number, migrate: boolean, accountPublicKey: GrumpkinAddress, newAccountPublicKey?: GrumpkinAddress, newSigningPubKey1?: GrumpkinAddress, newSigningPubKey2?: GrumpkinAddress, accountIndex?: number): Promise<Buffer>;
}
//# sourceMappingURL=index.d.ts.map