import { EthAddress } from '@aztec/barretenberg/address';
import { Database, DbAccount } from './database';
export declare class DexieDatabase implements Database {
    private dbName;
    private version;
    private db;
    private user;
    constructor(dbName?: string, version?: number);
    init(): Promise<void>;
    private createTables;
    clear(): Promise<void>;
    close(): Promise<void>;
    setAccount({ ethAddress, accountPublicKey }: DbAccount): Promise<void>;
    getAccount(ethAddress: EthAddress): Promise<DbAccount | undefined>;
    deleteAccount(ethAddress: EthAddress): Promise<void>;
}
//# sourceMappingURL=dexie_database.d.ts.map