import { EthAddress } from '@aztec/barretenberg/address';
import { Connection, ConnectionOptions } from 'typeorm';
import { Database, DbAccount } from '../database';
import { AccountDao } from './account_dao';
export declare const getOrmConfig: (dbPath?: string | undefined) => ConnectionOptions;
export declare class SQLDatabase implements Database {
    private connection;
    private accountRep;
    constructor(connection: Connection);
    init(): Promise<void>;
    close(): Promise<void>;
    clear(): Promise<void>;
    setAccount(account: DbAccount): Promise<void>;
    getAccount(ethAddress: EthAddress): Promise<AccountDao | undefined>;
    deleteAccount(ethAddress: EthAddress): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map